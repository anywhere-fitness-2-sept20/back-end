const db = require("../database/config");

function find() {}

function findById(id) {}

function add(newClass) {}

function update(changes, id) {}

function remove(id) {}

module.exports = {
  find,
  findById,
  add,
  update,
  remove,
};
